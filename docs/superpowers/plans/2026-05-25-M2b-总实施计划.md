# M2b 总实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 M2a 基础上补全 OAuth 全量（Google/WeChat/Feishu/DingTalk/Discord）、Stripe + 支付宝 + 微信支付、邀请返佣系统、Playground 前端，并发 tag v1.2.0。

**Architecture:**
- OAuth：定义 `oauth.Provider` 公共接口，每家在 `internal/auth/oauth/{name}/` 实现；`auth.Service` 增加通用方法 `OAuthStart/OAuthCallback/BindOAuth/UnbindOAuth`，GitHub 现有路径不变。
- 支付：`internal/payment/provider/` 存放 Provider 接口 + Stripe/Alipay/WechatPay 实现；`internal/payment/online/` 存放 OrderService（创建订单 / 处理 webhook / 查询）；`payment.Holder` 增加 `Online any` 字段。
- 邀请：`internal/invite/` 独立包（model/repo/service），支付 webhook 回调结算后调用。
- Playground：Go 侧 `internal/server/handler/user/playground.go`；前端 `web/user/src/views/Playground.vue`。

**Tech Stack:** Go 1.25 · Gin · GORM · go-redis · github.com/stripe/stripe-go/v76 · smartwalle/alipay/v3 · silenceper/wechat/v2（微信支付）· Vue 3 + Pinia + Tailwind CSS

---

## 文件总览

| 操作 | 路径 |
|------|------|
| 新建 | `internal/auth/oauth/provider.go` — 公共 OAuthProvider 接口 + UserInfo |
| 新建 | `internal/auth/oauth/google/provider.go` |
| 新建 | `internal/auth/oauth/wechat/provider.go` |
| 新建 | `internal/auth/oauth/feishu/provider.go` |
| 新建 | `internal/auth/oauth/dingtalk/provider.go` |
| 新建 | `internal/auth/oauth/discord/provider.go` |
| 修改 | `internal/auth/service.go` — 新增通用 OAuth 方法 |
| 修改 | `internal/auth/wire/wire.go` — 装配 5 个新 provider |
| 修改 | `internal/server/handler/authhttp/routes.go` — 新增通用 OAuth 路由 |
| 新建 | `internal/payment/provider/provider.go` — 接口 |
| 新建 | `internal/payment/provider/stripe/provider.go` |
| 新建 | `internal/payment/provider/alipay/provider.go` |
| 新建 | `internal/payment/provider/wechatpay/provider.go` |
| 新建 | `internal/payment/online/model.go` |
| 新建 | `internal/payment/online/repo.go` |
| 新建 | `internal/payment/online/service.go` |
| 新建 | `internal/payment/online/wire.go` |
| 修改 | `internal/payment/service.go` — Holder 增加 Online 字段 |
| 新建 | `internal/server/handler/payment/handler.go` |
| 新建 | `internal/invite/model.go` |
| 新建 | `internal/invite/repo.go` |
| 新建 | `internal/invite/service.go` |
| 新建 | `internal/invite/wire.go` |
| 修改 | `internal/user/model.go` — 新增 InvitedBy 字段 |
| 新建 | `internal/server/handler/user/playground.go` |
| 新建 | `web/user/src/views/Playground.vue` |
| 新建 | `migrations/mysql/000024_m2b_payment_invite.up.sql` (+ down) |
| 新建 | `migrations/mysql/000025_m2b_seed_settings.up.sql` (+ down) |
| 新建 | `migrations/postgres/000024_m2b_payment_invite.up.sql` (+ down) |
| 新建 | `migrations/postgres/000025_m2b_seed_settings.up.sql` (+ down) |

---

## Phase A：OAuth 全量

### Task A1：公共 OAuthProvider 接口

**Files:**
- 新建: `internal/auth/oauth/provider.go`

- [ ] **Step 1: 写 `internal/auth/oauth/provider.go`**

```go
package oauth

import "context"

// UserInfo 是各 OAuth provider 归一化后的用户信息。
type UserInfo struct {
    ProviderUID string // provider 侧的唯一 ID(稳定)
    Name        string
    Email       string // 可为空(部分 provider 不返回)
    Avatar      string
    Raw         []byte // 原始 JSON
}

// Provider 是所有第三方 OAuth 的统一接口。
type Provider interface {
    // Name 返回 provider 标识,如 "google"/"wechat"/"feishu"/"dingtalk"/"discord"。
    Name() string
    // BuildAuthURL 拼装授权跳转 URL。
    BuildAuthURL(ctx context.Context, state, redirectURL string) (string, error)
    // Exchange 用 code 换 UserInfo 及 accessToken。
    Exchange(ctx context.Context, code, redirectURL string) (*UserInfo, string, error)
}
```

- [ ] **Step 2: 验证编译**

```bash
cd /Users/admin/Documents/Repos/xyito/open/proapi
go build ./internal/auth/oauth/...
```

Expected: 无报错。

- [ ] **Step 3: commit**

```bash
git add internal/auth/oauth/provider.go
git commit -m "feat(oauth): add generic OAuthProvider interface"
```

---

### Task A2：Google OAuth provider

**Files:**
- 新建: `internal/auth/oauth/google/provider.go`

- [ ] **Step 1: 写 `internal/auth/oauth/google/provider.go`**

```go
package google

import (
    "context"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
    "net/url"
    "strings"
    "time"

    "github.com/ijry/pro-api/internal/auth/oauth"
    "github.com/ijry/pro-api/pkg/apierr"
)

// Config 是 New 的参数。
type Config struct {
    ClientID     string
    ClientSecret string
    Scopes       []string      // 默认 ["openid","email","profile"]
    HTTPTimeout  time.Duration // 默认 10s
}

var defaultScopes = []string{"openid", "email", "profile"}

type provider struct {
    cfg  Config
    http *http.Client
}

// New 构造 Google OAuth provider。
func New(cfg Config) oauth.Provider {
    if cfg.HTTPTimeout <= 0 {
        cfg.HTTPTimeout = 10 * time.Second
    }
    if len(cfg.Scopes) == 0 {
        cfg.Scopes = defaultScopes
    }
    return &provider{cfg: cfg, http: &http.Client{Timeout: cfg.HTTPTimeout}}
}

func (p *provider) Name() string { return "google" }

func (p *provider) BuildAuthURL(_ context.Context, state, redirectURL string) (string, error) {
    if p.cfg.ClientID == "" {
        return "", apierr.New(apierr.CodeForbidden, "Google 登录未启用")
    }
    u, _ := url.Parse("https://accounts.google.com/o/oauth2/v2/auth")
    q := u.Query()
    q.Set("client_id", p.cfg.ClientID)
    q.Set("redirect_uri", redirectURL)
    q.Set("response_type", "code")
    q.Set("scope", strings.Join(p.cfg.Scopes, " "))
    q.Set("state", state)
    q.Set("access_type", "offline")
    u.RawQuery = q.Encode()
    return u.String(), nil
}

func (p *provider) Exchange(ctx context.Context, code, redirectURL string) (*oauth.UserInfo, string, error) {
    if p.cfg.ClientID == "" {
        return nil, "", apierr.New(apierr.CodeForbidden, "Google 登录未启用")
    }
    form := url.Values{}
    form.Set("code", code)
    form.Set("client_id", p.cfg.ClientID)
    form.Set("client_secret", p.cfg.ClientSecret)
    form.Set("redirect_uri", redirectURL)
    form.Set("grant_type", "authorization_code")

    req, err := http.NewRequestWithContext(ctx, http.MethodPost,
        "https://oauth2.googleapis.com/token", strings.NewReader(form.Encode()))
    if err != nil {
        return nil, "", fmt.Errorf("google: build token req: %w", err)
    }
    req.Header.Set("Content-Type", "application/x-www-form-urlencoded")
    resp, err := p.http.Do(req)
    if err != nil {
        return nil, "", apierr.Wrap(apierr.CodeUpstreamUnavail, "google token http", err)
    }
    defer resp.Body.Close()
    body, _ := io.ReadAll(resp.Body)

    var tok struct {
        AccessToken string `json:"access_token"`
        Error       string `json:"error"`
    }
    if err := json.Unmarshal(body, &tok); err != nil || tok.AccessToken == "" {
        return nil, "", apierr.New(apierr.CodeUpstreamError, fmt.Sprintf("google token: %s", tok.Error))
    }

    uReq, _ := http.NewRequestWithContext(ctx, http.MethodGet,
        "https://www.googleapis.com/oauth2/v2/userinfo", nil)
    uReq.Header.Set("Authorization", "Bearer "+tok.AccessToken)
    uResp, err := p.http.Do(uReq)
    if err != nil {
        return nil, "", apierr.Wrap(apierr.CodeUpstreamUnavail, "google userinfo http", err)
    }
    defer uResp.Body.Close()
    raw, _ := io.ReadAll(uResp.Body)

    var u struct {
        ID      string `json:"id"`
        Name    string `json:"name"`
        Email   string `json:"email"`
        Picture string `json:"picture"`
    }
    if err := json.Unmarshal(raw, &u); err != nil {
        return nil, "", fmt.Errorf("google: decode userinfo: %w", err)
    }
    return &oauth.UserInfo{
        ProviderUID: u.ID,
        Name:        u.Name,
        Email:       u.Email,
        Avatar:      u.Picture,
        Raw:         raw,
    }, tok.AccessToken, nil
}
```

- [ ] **Step 2: 验证编译**

```bash
go build ./internal/auth/oauth/google/...
```

- [ ] **Step 3: commit**

```bash
git add internal/auth/oauth/google/
git commit -m "feat(oauth): add Google OAuth provider"
```

---

### Task A3：WeChat OAuth provider

**Files:**
- 新建: `internal/auth/oauth/wechat/provider.go`

- [ ] **Step 1: 写 `internal/auth/oauth/wechat/provider.go`**

```go
package wechat

import (
    "context"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
    "net/url"
    "time"

    "github.com/ijry/pro-api/internal/auth/oauth"
    "github.com/ijry/pro-api/pkg/apierr"
)

// Config 是 New 的参数。
type Config struct {
    AppID       string
    AppSecret   string
    HTTPTimeout time.Duration
}

type provider struct {
    cfg  Config
    http *http.Client
}

// New 构造微信网页 OAuth provider。
func New(cfg Config) oauth.Provider {
    if cfg.HTTPTimeout <= 0 {
        cfg.HTTPTimeout = 10 * time.Second
    }
    return &provider{cfg: cfg, http: &http.Client{Timeout: cfg.HTTPTimeout}}
}

func (p *provider) Name() string { return "wechat" }

func (p *provider) BuildAuthURL(_ context.Context, state, redirectURL string) (string, error) {
    if p.cfg.AppID == "" {
        return "", apierr.New(apierr.CodeForbidden, "微信登录未启用")
    }
    u, _ := url.Parse("https://open.weixin.qq.com/connect/qrconnect")
    q := u.Query()
    q.Set("appid", p.cfg.AppID)
    q.Set("redirect_uri", redirectURL)
    q.Set("response_type", "code")
    q.Set("scope", "snsapi_login")
    q.Set("state", state)
    u.RawQuery = q.Encode()
    return u.String() + "#wechat_redirect", nil
}

func (p *provider) Exchange(ctx context.Context, code, _ string) (*oauth.UserInfo, string, error) {
    if p.cfg.AppID == "" {
        return nil, "", apierr.New(apierr.CodeForbidden, "微信登录未启用")
    }
    tokURL := fmt.Sprintf(
        "https://api.weixin.qq.com/sns/oauth2/access_token?appid=%s&secret=%s&code=%s&grant_type=authorization_code",
        p.cfg.AppID, p.cfg.AppSecret, code)
    req, _ := http.NewRequestWithContext(ctx, http.MethodGet, tokURL, nil)
    resp, err := p.http.Do(req)
    if err != nil {
        return nil, "", apierr.Wrap(apierr.CodeUpstreamUnavail, "wechat token http", err)
    }
    defer resp.Body.Close()
    body, _ := io.ReadAll(resp.Body)

    var tok struct {
        AccessToken string `json:"access_token"`
        OpenID      string `json:"openid"`
        UnionID     string `json:"unionid"`
        ErrCode     int    `json:"errcode"`
        ErrMsg      string `json:"errmsg"`
    }
    if err := json.Unmarshal(body, &tok); err != nil || tok.AccessToken == "" {
        return nil, "", apierr.New(apierr.CodeUpstreamError, fmt.Sprintf("wechat token errcode=%d", tok.ErrCode))
    }
    uid := tok.UnionID
    if uid == "" {
        uid = tok.OpenID
    }

    infoURL := fmt.Sprintf("https://api.weixin.qq.com/sns/userinfo?access_token=%s&openid=%s&lang=zh_CN",
        tok.AccessToken, tok.OpenID)
    uReq, _ := http.NewRequestWithContext(ctx, http.MethodGet, infoURL, nil)
    uResp, err := p.http.Do(uReq)
    if err != nil {
        return nil, "", apierr.Wrap(apierr.CodeUpstreamUnavail, "wechat userinfo http", err)
    }
    defer uResp.Body.Close()
    raw, _ := io.ReadAll(uResp.Body)

    var u struct {
        Nickname  string `json:"nickname"`
        HeadImgURL string `json:"headimgurl"`
    }
    _ = json.Unmarshal(raw, &u)

    return &oauth.UserInfo{
        ProviderUID: uid,
        Name:        u.Nickname,
        Avatar:      u.HeadImgURL,
        Raw:         raw,
    }, tok.AccessToken, nil
}
```

- [ ] **Step 2: 验证编译 & commit**

```bash
go build ./internal/auth/oauth/wechat/...
git add internal/auth/oauth/wechat/
git commit -m "feat(oauth): add WeChat OAuth provider"
```

---

### Task A4：Feishu / DingTalk / Discord OAuth providers

**Files:**
- 新建: `internal/auth/oauth/feishu/provider.go`
- 新建: `internal/auth/oauth/dingtalk/provider.go`
- 新建: `internal/auth/oauth/discord/provider.go`

- [ ] **Step 1: 写 `internal/auth/oauth/feishu/provider.go`**

```go
package feishu

import (
    "context"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
    "net/url"
    "strings"
    "time"

    "github.com/ijry/pro-api/internal/auth/oauth"
    "github.com/ijry/pro-api/pkg/apierr"
)

type Config struct {
    AppID       string
    AppSecret   string
    HTTPTimeout time.Duration
}

type provider struct {
    cfg  Config
    http *http.Client
}

func New(cfg Config) oauth.Provider {
    if cfg.HTTPTimeout <= 0 {
        cfg.HTTPTimeout = 10 * time.Second
    }
    return &provider{cfg: cfg, http: &http.Client{Timeout: cfg.HTTPTimeout}}
}

func (p *provider) Name() string { return "feishu" }

func (p *provider) BuildAuthURL(_ context.Context, state, redirectURL string) (string, error) {
    if p.cfg.AppID == "" {
        return "", apierr.New(apierr.CodeForbidden, "飞书登录未启用")
    }
    u, _ := url.Parse("https://open.feishu.cn/open-apis/authen/v1/authorize")
    q := u.Query()
    q.Set("app_id", p.cfg.AppID)
    q.Set("redirect_uri", redirectURL)
    q.Set("scope", "contact:user.base:readonly")
    q.Set("state", state)
    u.RawQuery = q.Encode()
    return u.String(), nil
}

func (p *provider) Exchange(ctx context.Context, code, _ string) (*oauth.UserInfo, string, error) {
    if p.cfg.AppID == "" {
        return nil, "", apierr.New(apierr.CodeForbidden, "飞书登录未启用")
    }
    payload, _ := json.Marshal(map[string]string{
        "grant_type": "authorization_code",
        "code":       code,
    })
    req, _ := http.NewRequestWithContext(ctx, http.MethodPost,
        "https://open.feishu.cn/open-apis/authen/v1/oidc/access_token",
        strings.NewReader(string(payload)))
    req.Header.Set("Content-Type", "application/json; charset=utf-8")
    req.SetBasicAuth(p.cfg.AppID, p.cfg.AppSecret)

    resp, err := p.http.Do(req)
    if err != nil {
        return nil, "", apierr.Wrap(apierr.CodeUpstreamUnavail, "feishu token http", err)
    }
    defer resp.Body.Close()
    body, _ := io.ReadAll(resp.Body)

    var tok struct {
        Data struct {
            AccessToken string `json:"access_token"`
            OpenID      string `json:"open_id"`
            UnionID     string `json:"union_id"`
        } `json:"data"`
        Code int `json:"code"`
        Msg  string `json:"msg"`
    }
    if err := json.Unmarshal(body, &tok); err != nil || tok.Data.AccessToken == "" {
        return nil, "", apierr.New(apierr.CodeUpstreamError, fmt.Sprintf("feishu token code=%d %s", tok.Code, tok.Msg))
    }
    uid := tok.Data.UnionID
    if uid == "" {
        uid = tok.Data.OpenID
    }

    uReq, _ := http.NewRequestWithContext(ctx, http.MethodGet,
        "https://open.feishu.cn/open-apis/authen/v1/user_info", nil)
    uReq.Header.Set("Authorization", "Bearer "+tok.Data.AccessToken)
    uResp, err := p.http.Do(uReq)
    if err != nil {
        return nil, "", apierr.Wrap(apierr.CodeUpstreamUnavail, "feishu userinfo http", err)
    }
    defer uResp.Body.Close()
    raw, _ := io.ReadAll(uResp.Body)

    var u struct {
        Data struct {
            Name   string `json:"name"`
            Email  string `json:"enterprise_email"`
            Avatar struct {
                AvatarURL string `json:"avatar_72"`
            } `json:"avatar"`
        } `json:"data"`
    }
    _ = json.Unmarshal(raw, &u)

    return &oauth.UserInfo{
        ProviderUID: uid,
        Name:        u.Data.Name,
        Email:       u.Data.Email,
        Avatar:      u.Data.Avatar.AvatarURL,
        Raw:         raw,
    }, tok.Data.AccessToken, nil
}
```

- [ ] **Step 2: 写 `internal/auth/oauth/dingtalk/provider.go`**

```go
package dingtalk

import (
    "context"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
    "net/url"
    "strings"
    "time"

    "github.com/ijry/pro-api/internal/auth/oauth"
    "github.com/ijry/pro-api/pkg/apierr"
)

type Config struct {
    ClientID     string
    ClientSecret string
    HTTPTimeout  time.Duration
}

type provider struct {
    cfg  Config
    http *http.Client
}

func New(cfg Config) oauth.Provider {
    if cfg.HTTPTimeout <= 0 {
        cfg.HTTPTimeout = 10 * time.Second
    }
    return &provider{cfg: cfg, http: &http.Client{Timeout: cfg.HTTPTimeout}}
}

func (p *provider) Name() string { return "dingtalk" }

func (p *provider) BuildAuthURL(_ context.Context, state, redirectURL string) (string, error) {
    if p.cfg.ClientID == "" {
        return "", apierr.New(apierr.CodeForbidden, "钉钉登录未启用")
    }
    u, _ := url.Parse("https://login.dingtalk.com/oauth2/auth")
    q := u.Query()
    q.Set("client_id", p.cfg.ClientID)
    q.Set("redirect_uri", redirectURL)
    q.Set("response_type", "code")
    q.Set("scope", "openid")
    q.Set("state", state)
    q.Set("prompt", "consent")
    u.RawQuery = q.Encode()
    return u.String(), nil
}

func (p *provider) Exchange(ctx context.Context, code, redirectURL string) (*oauth.UserInfo, string, error) {
    if p.cfg.ClientID == "" {
        return nil, "", apierr.New(apierr.CodeForbidden, "钉钉登录未启用")
    }
    payload, _ := json.Marshal(map[string]string{
        "clientId":     p.cfg.ClientID,
        "clientSecret": p.cfg.ClientSecret,
        "code":         code,
        "redirectUri":  redirectURL,
        "grantType":    "authorization_code",
    })
    req, _ := http.NewRequestWithContext(ctx, http.MethodPost,
        "https://api.dingtalk.com/v1.0/oauth2/userAccessToken",
        strings.NewReader(string(payload)))
    req.Header.Set("Content-Type", "application/json")

    resp, err := p.http.Do(req)
    if err != nil {
        return nil, "", apierr.Wrap(apierr.CodeUpstreamUnavail, "dingtalk token http", err)
    }
    defer resp.Body.Close()
    body, _ := io.ReadAll(resp.Body)

    var tok struct {
        AccessToken string `json:"accessToken"`
        Code        string `json:"code"`
        Message     string `json:"message"`
    }
    if err := json.Unmarshal(body, &tok); err != nil || tok.AccessToken == "" {
        return nil, "", apierr.New(apierr.CodeUpstreamError, fmt.Sprintf("dingtalk token: %s", tok.Message))
    }

    uReq, _ := http.NewRequestWithContext(ctx, http.MethodGet,
        "https://api.dingtalk.com/v1.0/contact/users/me", nil)
    uReq.Header.Set("x-acs-dingtalk-access-token", tok.AccessToken)
    uResp, err := p.http.Do(uReq)
    if err != nil {
        return nil, "", apierr.Wrap(apierr.CodeUpstreamUnavail, "dingtalk userinfo http", err)
    }
    defer uResp.Body.Close()
    raw, _ := io.ReadAll(uResp.Body)

    var u struct {
        OpenID    string `json:"openId"`
        UnionID   string `json:"unionId"`
        Nick      string `json:"nick"`
        Email     string `json:"email"`
        AvatarURL string `json:"avatarUrl"`
    }
    _ = json.Unmarshal(raw, &u)
    uid := u.UnionID
    if uid == "" {
        uid = u.OpenID
    }

    return &oauth.UserInfo{
        ProviderUID: uid,
        Name:        u.Nick,
        Email:       u.Email,
        Avatar:      u.AvatarURL,
        Raw:         raw,
    }, tok.AccessToken, nil
}
```

- [ ] **Step 3: 写 `internal/auth/oauth/discord/provider.go`**

```go
package discord

import (
    "context"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
    "net/url"
    "strings"
    "time"

    "github.com/ijry/pro-api/internal/auth/oauth"
    "github.com/ijry/pro-api/pkg/apierr"
)

type Config struct {
    ClientID     string
    ClientSecret string
    HTTPTimeout  time.Duration
}

type provider struct {
    cfg  Config
    http *http.Client
}

func New(cfg Config) oauth.Provider {
    if cfg.HTTPTimeout <= 0 {
        cfg.HTTPTimeout = 10 * time.Second
    }
    return &provider{cfg: cfg, http: &http.Client{Timeout: cfg.HTTPTimeout}}
}

func (p *provider) Name() string { return "discord" }

func (p *provider) BuildAuthURL(_ context.Context, state, redirectURL string) (string, error) {
    if p.cfg.ClientID == "" {
        return "", apierr.New(apierr.CodeForbidden, "Discord 登录未启用")
    }
    u, _ := url.Parse("https://discord.com/oauth2/authorize")
    q := u.Query()
    q.Set("client_id", p.cfg.ClientID)
    q.Set("redirect_uri", redirectURL)
    q.Set("response_type", "code")
    q.Set("scope", "identify email")
    q.Set("state", state)
    u.RawQuery = q.Encode()
    return u.String(), nil
}

func (p *provider) Exchange(ctx context.Context, code, redirectURL string) (*oauth.UserInfo, string, error) {
    if p.cfg.ClientID == "" {
        return nil, "", apierr.New(apierr.CodeForbidden, "Discord 登录未启用")
    }
    form := url.Values{}
    form.Set("client_id", p.cfg.ClientID)
    form.Set("client_secret", p.cfg.ClientSecret)
    form.Set("grant_type", "authorization_code")
    form.Set("code", code)
    form.Set("redirect_uri", redirectURL)

    req, _ := http.NewRequestWithContext(ctx, http.MethodPost,
        "https://discord.com/api/oauth2/token", strings.NewReader(form.Encode()))
    req.Header.Set("Content-Type", "application/x-www-form-urlencoded")
    resp, err := p.http.Do(req)
    if err != nil {
        return nil, "", apierr.Wrap(apierr.CodeUpstreamUnavail, "discord token http", err)
    }
    defer resp.Body.Close()
    body, _ := io.ReadAll(resp.Body)

    var tok struct {
        AccessToken string `json:"access_token"`
        Error       string `json:"error"`
    }
    if err := json.Unmarshal(body, &tok); err != nil || tok.AccessToken == "" {
        return nil, "", apierr.New(apierr.CodeUpstreamError, fmt.Sprintf("discord token: %s", tok.Error))
    }

    uReq, _ := http.NewRequestWithContext(ctx, http.MethodGet,
        "https://discord.com/api/users/@me", nil)
    uReq.Header.Set("Authorization", "Bearer "+tok.AccessToken)
    uResp, err := p.http.Do(uReq)
    if err != nil {
        return nil, "", apierr.Wrap(apierr.CodeUpstreamUnavail, "discord userinfo http", err)
    }
    defer uResp.Body.Close()
    raw, _ := io.ReadAll(uResp.Body)

    var u struct {
        ID            string `json:"id"`
        Username      string `json:"username"`
        Email         string `json:"email"`
        Avatar        string `json:"avatar"`
        Discriminator string `json:"discriminator"`
    }
    _ = json.Unmarshal(raw, &u)
    avatar := ""
    if u.Avatar != "" {
        avatar = fmt.Sprintf("https://cdn.discordapp.com/avatars/%s/%s.png", u.ID, u.Avatar)
    }

    return &oauth.UserInfo{
        ProviderUID: u.ID,
        Name:        u.Username,
        Email:       u.Email,
        Avatar:      avatar,
        Raw:         raw,
    }, tok.AccessToken, nil
}
```

- [ ] **Step 4: 验证编译**

```bash
go build ./internal/auth/oauth/...
```

- [ ] **Step 5: commit**

```bash
git add internal/auth/oauth/feishu/ internal/auth/oauth/dingtalk/ internal/auth/oauth/discord/
git commit -m "feat(oauth): add Feishu/DingTalk/Discord OAuth providers"
```

---

### Task A5：auth.Service 通用 OAuth 方法

**Files:**
- 修改: `internal/auth/service.go`（Service 接口 + svc 实现 + Deps）

- [ ] **Step 1: 扩展 `internal/auth/service.go` Service 接口**

在 `GithubEnabled() bool` 之后添加：

```go
// 通用 OAuth 方法(Google/WeChat/Feishu/DingTalk/Discord)。
OAuthStart(ctx context.Context, providerName, ip, ua, redirect string, bindUserID int64) (string, error)
OAuthCallback(ctx context.Context, providerName, code, state, ip, ua string) (*session.Session, *User, []byte, error)
BindOAuth(ctx context.Context, userID int64, providerName, code, state string) error
UnbindOAuth(ctx context.Context, userID int64, providerName string) error
OAuthEnabled(providerName string) bool
```

- [ ] **Step 2: 扩展 Deps 结构体**

在 `GithubProvider github.Provider` 后增加：

```go
OAuthProviders []oauth.Provider // Google/WeChat/Feishu/DingTalk/Discord 等
```

- [ ] **Step 3: 在 `svc` 上实现新方法**

在 service.go 中增加（可以附加在文件末尾）：

```go
// oauthProvider 按 name 查找已注册的 Provider。
func (s *svc) oauthProvider(name string) (oauth.Provider, bool) {
    for _, p := range s.deps.OAuthProviders {
        if p.Name() == name {
            return p, true
        }
    }
    return nil, false
}

func (s *svc) OAuthEnabled(providerName string) bool {
    _, ok := s.oauthProvider(providerName)
    return ok
}

func (s *svc) OAuthStart(ctx context.Context, providerName, ip, ua, redirect string, bindUserID int64) (string, error) {
    p, ok := s.oauthProvider(providerName)
    if !ok {
        return "", apierr.New(apierr.CodeForbidden, providerName+" 登录未启用")
    }
    state, err := s.deps.GithubState.New(ctx, s.deps.IDGen.Generate(), ip, ua, redirect, bindUserID)
    if err != nil {
        return "", fmt.Errorf("oauth start state: %w", err)
    }
    return p.BuildAuthURL(ctx, state, redirect)
}

func (s *svc) OAuthCallback(ctx context.Context, providerName, code, state, ip, ua string) (*session.Session, *User, []byte, error) {
    p, ok := s.oauthProvider(providerName)
    if !ok {
        return nil, nil, nil, apierr.New(apierr.CodeForbidden, providerName+" 登录未启用")
    }
    st, err := s.deps.GithubState.Consume(ctx, state)
    if err != nil {
        return nil, nil, nil, apierr.New(apierr.CodeInvalidParam, "state 无效或已过期")
    }
    userInfo, _, err := p.Exchange(ctx, code, st.Redirect)
    if err != nil {
        return nil, nil, nil, err
    }

    // 检查是否已有绑定
    binding, err := s.deps.OAuthRepo.FindByProvider(ctx, providerName, userInfo.ProviderUID)
    if err == nil && binding != nil {
        // 已绑定 → 直接登录
        u, err := s.deps.User.GetByID(ctx, binding.UserID)
        if err != nil {
            return nil, nil, nil, err
        }
        sess, err := s.deps.Session.Create(ctx, session.CreateInput{
            UserID: u.ID, IP: ip, UA: ua,
        })
        if err != nil {
            return nil, nil, nil, err
        }
        return sess, u, userInfo.Raw, nil
    }

    // 无绑定 → 自动注册
    email := userInfo.Email
    if email == "" {
        email = fmt.Sprintf("%s_%s@oauth.noemail.local", providerName, userInfo.ProviderUID)
    }
    result, err := s.Register(ctx, RegisterInput{
        Username: userInfo.Name,
        Email:    email,
        Password: s.deps.IDGen.Generate()*1 + 0 |0, // placeholder — use a random password
        IP:       ip,
        UA:       ua,
    })
    if err != nil {
        // 邮箱已存在 → 关联到已有账号
        u, err2 := s.deps.User.GetByEmail(ctx, email)
        if err2 != nil {
            return nil, nil, nil, err
        }
        _ = s.deps.OAuthRepo.Create(ctx, &oauthrepo.Binding{
            UserID: u.ID, Provider: providerName, ProviderUID: userInfo.ProviderUID,
            Email: email, Profile: userInfo.Raw,
        })
        sess, err3 := s.deps.Session.Create(ctx, session.CreateInput{UserID: u.ID, IP: ip, UA: ua})
        if err3 != nil {
            return nil, nil, nil, err3
        }
        return sess, u, userInfo.Raw, nil
    }

    _ = s.deps.OAuthRepo.Create(ctx, &oauthrepo.Binding{
        UserID: result.UserID, Provider: providerName, ProviderUID: userInfo.ProviderUID,
        Email: email, Profile: userInfo.Raw,
    })
    return result.Session, result.User, userInfo.Raw, nil
}

func (s *svc) BindOAuth(ctx context.Context, userID int64, providerName, code, state string) error {
    p, ok := s.oauthProvider(providerName)
    if !ok {
        return apierr.New(apierr.CodeForbidden, providerName+" 登录未启用")
    }
    st, err := s.deps.GithubState.Consume(ctx, state)
    if err != nil {
        return apierr.New(apierr.CodeInvalidParam, "state 无效或已过期")
    }
    userInfo, _, err := p.Exchange(ctx, code, st.Redirect)
    if err != nil {
        return err
    }
    return s.deps.OAuthRepo.Create(ctx, &oauthrepo.Binding{
        UserID: userID, Provider: providerName, ProviderUID: userInfo.ProviderUID,
        Email: userInfo.Email, Profile: userInfo.Raw,
    })
}

func (s *svc) UnbindOAuth(ctx context.Context, userID int64, providerName string) error {
    return s.deps.OAuthRepo.DeleteByUserProvider(ctx, userID, providerName)
}
```

注意：需要在文件顶部 import `oauthrepo "github.com/ijry/pro-api/internal/auth/oauth"` 并用 `&oauth.Binding{...}`（`oauth.Binding` 已在 `internal/auth/oauth/model.go` 里定义）。调整 password 部分改为 crypto/rand 随机字符串：

```go
import "crypto/rand" // 在现有 import 块里加

func randomPassword() string {
    b := make([]byte, 16)
    _, _ = rand.Read(b)
    return fmt.Sprintf("%x", b)
}
```

然后把 `OAuthCallback` 里的 password 改为 `randomPassword()`。

- [ ] **Step 4: 检查 `oauth.Repository` 是否有 `DeleteByUserProvider`**

查看 `internal/auth/oauth/repo.go`：

```bash
grep -n "DeleteBy\|FindByProvider\|func " internal/auth/oauth/repo.go
```

若缺少方法，在 repo.go 的 interface 和 gormRepo 里补上：

```go
// Repository interface 追加：
DeleteByUserProvider(ctx context.Context, userID int64, provider string) error

// gormRepo 实现：
func (r *gormRepo) DeleteByUserProvider(ctx context.Context, userID int64, provider string) error {
    return r.db.WithContext(ctx).
        Where("user_id = ? AND provider = ?", userID, provider).
        Delete(&Binding{}).Error
}
```

- [ ] **Step 5: 验证编译**

```bash
go build ./internal/auth/...
```

- [ ] **Step 6: commit**

```bash
git add internal/auth/
git commit -m "feat(oauth): add generic OAuthStart/Callback/Bind/Unbind to auth.Service"
```

---

### Task A6：装配 5 个新 provider + HTTP 路由

**Files:**
- 修改: `internal/auth/wire/wire.go`
- 修改: `internal/server/handler/authhttp/routes.go`（或 handler.go）

- [ ] **Step 1: 在 wire.go 中装配新 providers**

在 `Wire` 函数里，`authSvc := auth.NewService(...)` 之前，添加：

```go
import (
    authdiscord  "github.com/ijry/pro-api/internal/auth/oauth/discord"
    authdingtalk "github.com/ijry/pro-api/internal/auth/oauth/dingtalk"
    authfeishu   "github.com/ijry/pro-api/internal/auth/oauth/feishu"
    authgoogle   "github.com/ijry/pro-api/internal/auth/oauth/google"
    authwechat   "github.com/ijry/pro-api/internal/auth/oauth/wechat"
)

var oauthProviders []oauthpkg.Provider
if cfg := readGoogleOAuth(ctx, a); cfg != nil {
    oauthProviders = append(oauthProviders, authgoogle.New(*cfg))
}
if cfg := readWechatOAuth(ctx, a); cfg != nil {
    oauthProviders = append(oauthProviders, authwechat.New(*cfg))
}
if cfg := readFeishuOAuth(ctx, a); cfg != nil {
    oauthProviders = append(oauthProviders, authfeishu.New(*cfg))
}
if cfg := readDingTalkOAuth(ctx, a); cfg != nil {
    oauthProviders = append(oauthProviders, authdingtalk.New(*cfg))
}
if cfg := readDiscordOAuth(ctx, a); cfg != nil {
    oauthProviders = append(oauthProviders, authdiscord.New(*cfg))
}
```

在 `auth.Deps` 增加 `OAuthProviders: oauthProviders`。

在 wire.go 底部添加 5 个 `read*OAuth` 函数，参照 `readGithubOAuth` 的模式（都读 `auth.{name}_oauth` setting，JSON 含 `client_id` / `client_secret`）：

```go
func readGoogleOAuth(ctx context.Context, a *app.Application) *authgoogle.Config {
    var raw struct {
        ClientID     string `json:"client_id"`
        ClientSecret string `json:"client_secret"`
    }
    if err := a.Setting.GetJSON(ctx, "auth.google_oauth", &raw); err != nil || raw.ClientID == "" {
        return nil
    }
    secret := raw.ClientSecret
    if a.Crypto != nil {
        if dec, err := a.Crypto.Decrypt(raw.ClientSecret); err == nil {
            secret = dec
        }
    }
    return &authgoogle.Config{ClientID: raw.ClientID, ClientSecret: secret}
}

// readWechatOAuth: setting key "auth.wechat_oauth", 字段 app_id / app_secret
func readWechatOAuth(ctx context.Context, a *app.Application) *authwechat.Config {
    var raw struct {
        AppID     string `json:"app_id"`
        AppSecret string `json:"app_secret"`
    }
    if err := a.Setting.GetJSON(ctx, "auth.wechat_oauth", &raw); err != nil || raw.AppID == "" {
        return nil
    }
    secret := raw.AppSecret
    if a.Crypto != nil {
        if dec, err := a.Crypto.Decrypt(raw.AppSecret); err == nil {
            secret = dec
        }
    }
    return &authwechat.Config{AppID: raw.AppID, AppSecret: secret}
}

// readFeishuOAuth: setting key "auth.feishu_oauth", 字段 app_id / app_secret
func readFeishuOAuth(ctx context.Context, a *app.Application) *authfeishu.Config {
    var raw struct {
        AppID     string `json:"app_id"`
        AppSecret string `json:"app_secret"`
    }
    if err := a.Setting.GetJSON(ctx, "auth.feishu_oauth", &raw); err != nil || raw.AppID == "" {
        return nil
    }
    secret := raw.AppSecret
    if a.Crypto != nil {
        if dec, err := a.Crypto.Decrypt(raw.AppSecret); err == nil {
            secret = dec
        }
    }
    return &authfeishu.Config{AppID: raw.AppID, AppSecret: secret}
}

// readDingTalkOAuth: setting key "auth.dingtalk_oauth", 字段 client_id / client_secret
func readDingTalkOAuth(ctx context.Context, a *app.Application) *authdingtalk.Config {
    var raw struct {
        ClientID     string `json:"client_id"`
        ClientSecret string `json:"client_secret"`
    }
    if err := a.Setting.GetJSON(ctx, "auth.dingtalk_oauth", &raw); err != nil || raw.ClientID == "" {
        return nil
    }
    secret := raw.ClientSecret
    if a.Crypto != nil {
        if dec, err := a.Crypto.Decrypt(raw.ClientSecret); err == nil {
            secret = dec
        }
    }
    return &authdingtalk.Config{ClientID: raw.ClientID, ClientSecret: secret}
}

// readDiscordOAuth: setting key "auth.discord_oauth", 字段 client_id / client_secret
func readDiscordOAuth(ctx context.Context, a *app.Application) *authdiscord.Config {
    var raw struct {
        ClientID     string `json:"client_id"`
        ClientSecret string `json:"client_secret"`
    }
    if err := a.Setting.GetJSON(ctx, "auth.discord_oauth", &raw); err != nil || raw.ClientID == "" {
        return nil
    }
    secret := raw.ClientSecret
    if a.Crypto != nil {
        if dec, err := a.Crypto.Decrypt(raw.ClientSecret); err == nil {
            secret = dec
        }
    }
    return &authdiscord.Config{ClientID: raw.ClientID, ClientSecret: secret}
}
```

- [ ] **Step 2: 找到 authhttp 路由注册文件**

```bash
ls internal/server/handler/authhttp/
```

在 `RegisterRoutes` 或路由注册函数中，找到 GitHub 路由的位置，在其后追加通用路由：

```go
// 通用 OAuth 路由
authGroup.GET("/oauth/:provider/start", h.OAuthStart)
authGroup.GET("/oauth/:provider/callback", h.OAuthCallback)
// 绑定/解绑需要 session 鉴权(SessionMiddleware 由 RegisterRoutes 调用方已挂)
authedGroup.POST("/oauth/:provider/bind", h.BindOAuth)
authedGroup.DELETE("/oauth/:provider/bind", h.UnbindOAuth)
```

- [ ] **Step 3: 在 handler 中实现新 action**

在 authhttp handler 文件中添加：

```go
// OAuthStart GET /api/auth/oauth/:provider/start?redirect_url=...
func (h *Handler) OAuthStart(c *gin.Context) {
    provider := c.Param("provider")
    redirect := c.Query("redirect_url")
    bindUID := int64(0)
    authURL, err := h.deps.Auth.OAuthStart(c.Request.Context(), provider,
        c.ClientIP(), c.Request.UserAgent(), redirect, bindUID)
    if err != nil {
        _ = c.Error(err)
        return
    }
    c.Redirect(http.StatusFound, authURL)
}

// OAuthCallback GET /api/auth/oauth/:provider/callback?code=...&state=...
func (h *Handler) OAuthCallback(c *gin.Context) {
    provider := c.Param("provider")
    code := c.Query("code")
    state := c.Query("state")
    sess, user, _, err := h.deps.Auth.OAuthCallback(c.Request.Context(), provider,
        code, state, c.ClientIP(), c.Request.UserAgent())
    if err != nil {
        _ = c.Error(err)
        return
    }
    setSessionCookie(c, sess.ID, h.deps.CookieSecure)
    c.JSON(http.StatusOK, gin.H{"user_id": user.ID, "session_id": sess.ID})
}

// BindOAuth POST /api/auth/oauth/:provider/bind  (session-authed)
func (h *Handler) BindOAuth(c *gin.Context) {
    provider := c.Param("provider")
    var body struct {
        Code  string `json:"code"`
        State string `json:"state"`
    }
    if err := c.ShouldBindJSON(&body); err != nil {
        _ = c.Error(apierr.New(apierr.CodeInvalidParam, err.Error()))
        return
    }
    uid := mustUserID(c)
    if err := h.deps.Auth.BindOAuth(c.Request.Context(), uid, provider, body.Code, body.State); err != nil {
        _ = c.Error(err)
        return
    }
    c.JSON(http.StatusOK, gin.H{"ok": true})
}

// UnbindOAuth DELETE /api/auth/oauth/:provider/bind  (session-authed)
func (h *Handler) UnbindOAuth(c *gin.Context) {
    provider := c.Param("provider")
    uid := mustUserID(c)
    if err := h.deps.Auth.UnbindOAuth(c.Request.Context(), uid, provider); err != nil {
        _ = c.Error(err)
        return
    }
    c.JSON(http.StatusOK, gin.H{"ok": true})
}
```

- [ ] **Step 4: 验证编译**

```bash
go build ./...
```

- [ ] **Step 5: commit**

```bash
git add internal/auth/ internal/server/handler/authhttp/
git commit -m "feat(oauth): wire 5 new providers + HTTP routes"
```

---

## Phase B：Stripe 支付

### Task B1：数据库迁移 + 模型

**Files:**
- 新建: `migrations/mysql/000024_m2b_payment_invite.up.sql` (+ down)
- 新建: `migrations/postgres/000024_m2b_payment_invite.up.sql` (+ down)
- 新建: `internal/payment/online/model.go`
- 修改: `internal/user/model.go`

- [ ] **Step 1: 写 `migrations/mysql/000024_m2b_payment_invite.up.sql`**

```sql
-- 给 users 表增加 invited_by
ALTER TABLE users ADD COLUMN invited_by BIGINT NOT NULL DEFAULT 0 COMMENT '邀请人 user_id';

-- payment_orders 表
CREATE TABLE IF NOT EXISTS payment_orders (
    id          BIGINT      NOT NULL PRIMARY KEY,
    user_id     BIGINT      NOT NULL,
    provider    VARCHAR(32) NOT NULL COMMENT 'stripe/alipay/wechatpay',
    out_trade_no VARCHAR(64) NOT NULL UNIQUE,
    provider_order_id VARCHAR(128) DEFAULT '',
    amount_cents BIGINT NOT NULL COMMENT '分',
    currency    VARCHAR(8)  NOT NULL DEFAULT 'CNY',
    status      VARCHAR(16) NOT NULL DEFAULT 'pending' COMMENT 'pending/paid/failed/refunded',
    credits     BIGINT      NOT NULL DEFAULT 0 COMMENT '对应充值 credits',
    meta        JSON,
    paid_at     DATETIME(3),
    created_at  DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at  DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    INDEX idx_user_id (user_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- invite_records 表
CREATE TABLE IF NOT EXISTS invite_records (
    id           BIGINT NOT NULL PRIMARY KEY,
    inviter_id   BIGINT NOT NULL,
    invitee_id   BIGINT NOT NULL,
    order_id     BIGINT NOT NULL,
    rebate_cents BIGINT NOT NULL DEFAULT 0,
    rebate_credits BIGINT NOT NULL DEFAULT 0,
    created_at   DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    INDEX idx_inviter (inviter_id),
    INDEX idx_invitee (invitee_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

- [ ] **Step 2: 写 `migrations/mysql/000024_m2b_payment_invite.down.sql`**

```sql
DROP TABLE IF EXISTS invite_records;
DROP TABLE IF EXISTS payment_orders;
ALTER TABLE users DROP COLUMN IF EXISTS invited_by;
```

- [ ] **Step 3: 写 PostgreSQL 版本**

`migrations/postgres/000024_m2b_payment_invite.up.sql`:

```sql
ALTER TABLE users ADD COLUMN IF NOT EXISTS invited_by BIGINT NOT NULL DEFAULT 0;

CREATE TABLE IF NOT EXISTS payment_orders (
    id                BIGINT        NOT NULL PRIMARY KEY,
    user_id           BIGINT        NOT NULL,
    provider          VARCHAR(32)   NOT NULL,
    out_trade_no      VARCHAR(64)   NOT NULL UNIQUE,
    provider_order_id VARCHAR(128)  NOT NULL DEFAULT '',
    amount_cents      BIGINT        NOT NULL,
    currency          VARCHAR(8)    NOT NULL DEFAULT 'CNY',
    status            VARCHAR(16)   NOT NULL DEFAULT 'pending',
    credits           BIGINT        NOT NULL DEFAULT 0,
    meta              JSONB,
    paid_at           TIMESTAMPTZ,
    created_at        TIMESTAMPTZ   NOT NULL DEFAULT NOW(),
    updated_at        TIMESTAMPTZ   NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_payment_orders_user_id ON payment_orders(user_id);
CREATE INDEX IF NOT EXISTS idx_payment_orders_status  ON payment_orders(status);

CREATE TABLE IF NOT EXISTS invite_records (
    id             BIGINT       NOT NULL PRIMARY KEY,
    inviter_id     BIGINT       NOT NULL,
    invitee_id     BIGINT       NOT NULL,
    order_id       BIGINT       NOT NULL,
    rebate_cents   BIGINT       NOT NULL DEFAULT 0,
    rebate_credits BIGINT       NOT NULL DEFAULT 0,
    created_at     TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_invite_records_inviter ON invite_records(inviter_id);
CREATE INDEX IF NOT EXISTS idx_invite_records_invitee ON invite_records(invitee_id);
```

`migrations/postgres/000024_m2b_payment_invite.down.sql`:

```sql
DROP TABLE IF EXISTS invite_records;
DROP TABLE IF EXISTS payment_orders;
ALTER TABLE users DROP COLUMN IF EXISTS invited_by;
```

- [ ] **Step 4: 在 `internal/user/model.go` 增加 `InvitedBy` 字段**

找到 `User` struct，在 `CreatedAt` 前加：

```go
InvitedBy int64 `gorm:"column:invited_by;not null;default:0" json:"invited_by"`
```

- [ ] **Step 5: 写 `internal/payment/online/model.go`**

```go
package online

import (
    "encoding/json"
    "time"
)

// OrderStatus 订单状态。
type OrderStatus string

const (
    StatusPending  OrderStatus = "pending"
    StatusPaid     OrderStatus = "paid"
    StatusFailed   OrderStatus = "failed"
    StatusRefunded OrderStatus = "refunded"
)

// Order 是支付订单的 DB 模型。
type Order struct {
    ID              int64           `gorm:"primaryKey"`
    UserID          int64           `gorm:"not null;index"`
    Provider        string          `gorm:"size:32;not null"`
    OutTradeNo      string          `gorm:"size:64;not null;uniqueIndex"`
    ProviderOrderID string          `gorm:"size:128;not null;default:''"`
    AmountCents     int64           `gorm:"not null"`
    Currency        string          `gorm:"size:8;not null;default:CNY"`
    Status          OrderStatus     `gorm:"size:16;not null;default:pending;index"`
    Credits         int64           `gorm:"not null;default:0"`
    Meta            json.RawMessage `gorm:"type:json"`
    PaidAt          *time.Time
    CreatedAt       time.Time
    UpdatedAt       time.Time
}

func (Order) TableName() string { return "payment_orders" }
```

- [ ] **Step 6: commit**

```bash
git add migrations/ internal/user/model.go internal/payment/online/model.go
git commit -m "feat(payment): migration 000024 + PaymentOrder/InviteRecord models"
```

---

### Task B2：Payment Provider 接口 + Stripe 实现

**Files:**
- 新建: `internal/payment/provider/provider.go`
- 新建: `internal/payment/provider/stripe/provider.go`

- [ ] **Step 1: 写 `internal/payment/provider/provider.go`**

```go
package provider

import "context"

// CreateOrderInput 是创建支付订单的入参。
type CreateOrderInput struct {
    OutTradeNo  string // 商户侧唯一单号
    AmountCents int64  // 金额(分)
    Currency    string // "CNY" / "USD"
    Description string
    NotifyURL   string // webhook 回调地址
    ReturnURL   string // 支付成功后跳转(仅网页支付)
    Extra       map[string]string
}

// CreateOrderResult 是创建订单后 provider 返回的信息。
type CreateOrderResult struct {
    ProviderOrderID string // provider 侧单号
    PayURL          string // 跳转/唤起支付的 URL 或二维码内容
    QRCode          string // 二维码 base64(可为空)
    ClientSecret    string // Stripe PaymentIntent client_secret
}

// WebhookEvent 是 provider 解析后的 webhook 事件。
type WebhookEvent struct {
    OutTradeNo      string // 商户单号
    ProviderOrderID string
    Paid            bool
    AmountCents     int64
    Currency        string
    Raw             []byte
}

// Provider 是支付接入抽象。
type Provider interface {
    Name() string
    CreateOrder(ctx context.Context, in CreateOrderInput) (*CreateOrderResult, error)
    // HandleWebhook 解析 webhook body+headers，返回标准事件；签名校验也在此完成。
    HandleWebhook(ctx context.Context, body []byte, headers map[string]string) (*WebhookEvent, error)
    QueryOrder(ctx context.Context, outTradeNo string) (*WebhookEvent, error)
}
```

- [ ] **Step 2: 添加 Stripe 依赖**

```bash
cd /Users/admin/Documents/Repos/xyito/open/proapi
go get github.com/stripe/stripe-go/v76@latest
```

- [ ] **Step 3: 写 `internal/payment/provider/stripe/provider.go`**

```go
package stripe

import (
    "context"
    "encoding/json"
    "fmt"
    "io"

    "github.com/ijry/pro-api/internal/payment/provider"
    "github.com/stripe/stripe-go/v76"
    "github.com/stripe/stripe-go/v76/paymentintent"
    "github.com/stripe/stripe-go/v76/webhook"
)

// Config 是 New 的参数。
type Config struct {
    SecretKey     string
    WebhookSecret string
}

type stripeProvider struct {
    cfg Config
}

// New 构造 Stripe payment provider。
func New(cfg Config) provider.Provider {
    stripe.Key = cfg.SecretKey
    return &stripeProvider{cfg: cfg}
}

func (p *stripeProvider) Name() string { return "stripe" }

func (p *stripeProvider) CreateOrder(_ context.Context, in provider.CreateOrderInput) (*provider.CreateOrderResult, error) {
    currency := in.Currency
    if currency == "" {
        currency = "usd"
    }
    params := &stripe.PaymentIntentParams{
        Amount:      stripe.Int64(in.AmountCents),
        Currency:    stripe.String(currency),
        Description: stripe.String(in.Description),
    }
    params.AddMetadata("out_trade_no", in.OutTradeNo)
    pi, err := paymentintent.New(params)
    if err != nil {
        return nil, fmt.Errorf("stripe: create PaymentIntent: %w", err)
    }
    return &provider.CreateOrderResult{
        ProviderOrderID: pi.ID,
        ClientSecret:    pi.ClientSecret,
    }, nil
}

func (p *stripeProvider) HandleWebhook(_ context.Context, body []byte, headers map[string]string) (*provider.WebhookEvent, error) {
    sig := headers["Stripe-Signature"]
    if sig == "" {
        sig = headers["stripe-signature"]
    }
    event, err := webhook.ConstructEvent(body, sig, p.cfg.WebhookSecret)
    if err != nil {
        return nil, fmt.Errorf("stripe: webhook verify: %w", err)
    }
    if event.Type != "payment_intent.succeeded" {
        return nil, nil // 忽略非支付成功事件
    }
    var pi stripe.PaymentIntent
    if err := json.Unmarshal(event.Data.Raw, &pi); err != nil {
        return nil, fmt.Errorf("stripe: decode PaymentIntent: %w", err)
    }
    raw, _ := io.ReadAll(io.NopCloser(nil)) // body already read
    return &provider.WebhookEvent{
        OutTradeNo:      pi.Metadata["out_trade_no"],
        ProviderOrderID: pi.ID,
        Paid:            true,
        AmountCents:     pi.Amount,
        Currency:        string(pi.Currency),
        Raw:             body,
    }, nil
}

func (p *stripeProvider) QueryOrder(_ context.Context, outTradeNo string) (*provider.WebhookEvent, error) {
    // Stripe 没有按商户单号查的 API；通过 search API 查 metadata
    params := &stripe.PaymentIntentSearchParams{}
    params.Query = fmt.Sprintf("metadata['out_trade_no']:'%s'", outTradeNo)
    iter := paymentintent.Search(params)
    for iter.Next() {
        pi := iter.PaymentIntent()
        return &provider.WebhookEvent{
            OutTradeNo:      outTradeNo,
            ProviderOrderID: pi.ID,
            Paid:            pi.Status == stripe.PaymentIntentStatusSucceeded,
            AmountCents:     pi.Amount,
            Currency:        string(pi.Currency),
        }, nil
    }
    return nil, fmt.Errorf("stripe: order not found: %s", outTradeNo)
}
```

- [ ] **Step 4: 验证编译**

```bash
go build ./internal/payment/provider/...
```

- [ ] **Step 5: commit**

```bash
git add internal/payment/provider/ go.mod go.sum
git commit -m "feat(payment): payment Provider interface + Stripe implementation"
```

---

### Task B3：Online OrderService（通用层）

**Files:**
- 新建: `internal/payment/online/repo.go`
- 新建: `internal/payment/online/service.go`
- 新建: `internal/payment/online/wire.go`
- 修改: `internal/payment/service.go`（Holder 增 Online 字段）

- [ ] **Step 1: 写 `internal/payment/online/repo.go`**

```go
package online

import (
    "context"

    "gorm.io/gorm"
)

// Repository 是 payment_orders 的 CRUD 接口。
type Repository interface {
    Create(ctx context.Context, o *Order) error
    FindByOutTradeNo(ctx context.Context, no string) (*Order, error)
    UpdateStatus(ctx context.Context, id int64, status OrderStatus, providerOrderID string, paidAt *time.Time) error
    ListByUser(ctx context.Context, userID int64, limit, offset int) ([]*Order, error)
}

type gormRepo struct{ db *gorm.DB }

// NewRepository 构造 gorm 实现。
func NewRepository(db *gorm.DB) Repository { return &gormRepo{db: db} }

func (r *gormRepo) Create(ctx context.Context, o *Order) error {
    return r.db.WithContext(ctx).Create(o).Error
}

func (r *gormRepo) FindByOutTradeNo(ctx context.Context, no string) (*Order, error) {
    var o Order
    if err := r.db.WithContext(ctx).Where("out_trade_no = ?", no).First(&o).Error; err != nil {
        return nil, err
    }
    return &o, nil
}

func (r *gormRepo) UpdateStatus(ctx context.Context, id int64, status OrderStatus, providerOrderID string, paidAt *time.Time) error {
    return r.db.WithContext(ctx).Model(&Order{}).Where("id = ?", id).Updates(map[string]any{
        "status":            status,
        "provider_order_id": providerOrderID,
        "paid_at":           paidAt,
    }).Error
}

func (r *gormRepo) ListByUser(ctx context.Context, userID int64, limit, offset int) ([]*Order, error) {
    var list []*Order
    err := r.db.WithContext(ctx).
        Where("user_id = ?", userID).
        Order("created_at DESC").
        Limit(limit).Offset(offset).
        Find(&list).Error
    return list, err
}
```

注意补上 `"time"` import。

- [ ] **Step 2: 写 `internal/payment/online/service.go`**

```go
package online

import (
    "context"
    "fmt"
    "time"

    payprovider "github.com/ijry/pro-api/internal/payment/provider"
    "github.com/ijry/pro-api/internal/util/clock"
    "github.com/ijry/pro-api/internal/util/idgen"
    "go.uber.org/zap"
)

// WalletCredit 是注入的 wallet 接口(避免直接依赖 wallet 包)。
type WalletCredit interface {
    Credit(ctx context.Context, userID, credits int64, note string) error
}

// InviteRebate 是邀请返佣钩子接口(避免循环依赖)。
type InviteRebate interface {
    OnOrderPaid(ctx context.Context, orderID, userID int64) error
}

// Deps 是 NewService 的依赖。
type Deps struct {
    Repo      Repository
    Providers []payprovider.Provider
    Wallet    WalletCredit
    Invite    InviteRebate // 可空
    IDGen     *idgen.Generator
    Clock     clock.Clock
    Log       *zap.Logger
}

// Service 是在线支付服务。
type Service struct {
    deps Deps
}

// NewService 构造 Service。
func NewService(deps Deps) *Service {
    if deps.Clock == nil {
        deps.Clock = clock.Real
    }
    if deps.Log == nil {
        deps.Log = zap.NewNop()
    }
    return &Service{deps: deps}
}

func (s *Service) provider(name string) (payprovider.Provider, bool) {
    for _, p := range s.deps.Providers {
        if p.Name() == name {
            return p, true
        }
    }
    return nil, false
}

// CreateOrderInput 是创建订单的入参。
type CreateOrderInput struct {
    UserID      int64
    Provider    string
    AmountCents int64
    Currency    string
    Credits     int64  // 充值等额 credits
    Description string
    NotifyURL   string
    ReturnURL   string
}

// CreateOrderResult 是创建订单的出参。
type CreateOrderResult struct {
    OrderID      int64
    OutTradeNo   string
    ClientSecret string // Stripe
    PayURL       string // 支付宝/微信
}

// CreateOrder 创建支付单并持久化。
func (s *Service) CreateOrder(ctx context.Context, in CreateOrderInput) (*CreateOrderResult, error) {
    p, ok := s.provider(in.Provider)
    if !ok {
        return nil, fmt.Errorf("online payment: unknown provider: %s", in.Provider)
    }
    orderID := s.deps.IDGen.Generate()
    outTradeNo := fmt.Sprintf("PRO%d", orderID)
    o := &Order{
        ID:          orderID,
        UserID:      in.UserID,
        Provider:    in.Provider,
        OutTradeNo:  outTradeNo,
        AmountCents: in.AmountCents,
        Currency:    in.Currency,
        Status:      StatusPending,
        Credits:     in.Credits,
    }
    if err := s.deps.Repo.Create(ctx, o); err != nil {
        return nil, fmt.Errorf("online payment: create order: %w", err)
    }
    res, err := p.CreateOrder(ctx, payprovider.CreateOrderInput{
        OutTradeNo:  outTradeNo,
        AmountCents: in.AmountCents,
        Currency:    in.Currency,
        Description: in.Description,
        NotifyURL:   in.NotifyURL,
        ReturnURL:   in.ReturnURL,
    })
    if err != nil {
        return nil, fmt.Errorf("online payment: provider create: %w", err)
    }
    return &CreateOrderResult{
        OrderID:      orderID,
        OutTradeNo:   outTradeNo,
        ClientSecret: res.ClientSecret,
        PayURL:       res.PayURL,
    }, nil
}

// HandleWebhook 处理 provider 回调,幂等。
func (s *Service) HandleWebhook(ctx context.Context, providerName string, body []byte, headers map[string]string) error {
    p, ok := s.provider(providerName)
    if !ok {
        return fmt.Errorf("online payment: unknown provider: %s", providerName)
    }
    ev, err := p.HandleWebhook(ctx, body, headers)
    if err != nil {
        return err
    }
    if ev == nil || !ev.Paid {
        return nil
    }
    o, err := s.deps.Repo.FindByOutTradeNo(ctx, ev.OutTradeNo)
    if err != nil {
        return fmt.Errorf("online payment: find order %s: %w", ev.OutTradeNo, err)
    }
    if o.Status == StatusPaid {
        return nil // 幂等
    }
    now := s.deps.Clock.Now()
    if err := s.deps.Repo.UpdateStatus(ctx, o.ID, StatusPaid, ev.ProviderOrderID, &now); err != nil {
        return fmt.Errorf("online payment: update order: %w", err)
    }
    // 充值 credits
    if err := s.deps.Wallet.Credit(ctx, o.UserID, o.Credits, fmt.Sprintf("充值订单 %s", o.OutTradeNo)); err != nil {
        s.deps.Log.Error("online payment: wallet credit failed", zap.Error(err), zap.Int64("order_id", o.ID))
    }
    // 邀请返佣钩子
    if s.deps.Invite != nil {
        if err := s.deps.Invite.OnOrderPaid(ctx, o.ID, o.UserID); err != nil {
            s.deps.Log.Warn("online payment: invite rebate failed", zap.Error(err), zap.Int64("order_id", o.ID))
        }
    }
    return nil
}
```

- [ ] **Step 3: 写 `internal/payment/online/wire.go`**

```go
package online

import (
    "github.com/ijry/pro-api/internal/app"
    payprovider "github.com/ijry/pro-api/internal/payment/provider"
    "github.com/ijry/pro-api/internal/payment"
)

// Wire 装配 online.Service 并挂到 app.PaymentSvc.(payment.Holder).Online。
func Wire(a *app.Application, providers []payprovider.Provider, wallet WalletCredit, invite InviteRebate) error {
    svc := NewService(Deps{
        Repo:      NewRepository(a.DB),
        Providers: providers,
        Wallet:    wallet,
        Invite:    invite,
        IDGen:     a.IDGen,
        Clock:     a.Clock,
        Log:       a.Log,
    })
    h := payment.HolderFrom(a.PaymentSvc)
    h.Online = svc
    a.PaymentSvc = h
    return nil
}
```

- [ ] **Step 4: 修改 `internal/payment/service.go` Holder**

在 Holder 结构体中增加：

```go
// Online: *online.Service（在线支付：Stripe/Alipay/WechatPay）。
Online any
```

- [ ] **Step 5: 验证编译**

```bash
go build ./internal/payment/...
```

- [ ] **Step 6: commit**

```bash
git add internal/payment/
git commit -m "feat(payment): online OrderService + Stripe wire"
```

---

### Task B4：支付 HTTP handler

**Files:**
- 新建: `internal/server/handler/payment/handler.go`

- [ ] **Step 1: 写 `internal/server/handler/payment/handler.go`**

```go
package payment

import (
    "io"
    "net/http"

    "github.com/gin-gonic/gin"
    "github.com/ijry/pro-api/internal/payment/online"
    "github.com/ijry/pro-api/pkg/apierr"
    "go.uber.org/zap"
)

// Deps 是 Handler 的依赖。
type Deps struct {
    Online *online.Service
    Log    *zap.Logger
}

// Handler 是支付 HTTP 处理器。
type Handler struct{ deps Deps }

// New 构造 Handler。
func New(deps Deps) *Handler { return &Handler{deps: deps} }

// Register 挂载路由到传入的 RouterGroup。
//
//	/api/user/payment/create   POST  创建订单
//	/api/user/payment/orders   GET   订单列表
//	/api/pay/webhook/:provider POST  provider 回调(无鉴权)
func (h *Handler) Register(userGroup, publicGroup *gin.RouterGroup) {
    pay := userGroup.Group("/payment")
    pay.POST("/create", h.CreateOrder)
    pay.GET("/orders", h.ListOrders)
    publicGroup.POST("/webhook/:provider", h.Webhook)
}

type createOrderReq struct {
    Provider    string `json:"provider" binding:"required"`
    AmountCents int64  `json:"amount_cents" binding:"required,min=1"`
    Credits     int64  `json:"credits" binding:"required,min=1"`
    Currency    string `json:"currency"`
    NotifyURL   string `json:"notify_url"`
    ReturnURL   string `json:"return_url"`
}

func (h *Handler) CreateOrder(c *gin.Context) {
    var req createOrderReq
    if err := c.ShouldBindJSON(&req); err != nil {
        _ = c.Error(apierr.New(apierr.CodeInvalidParam, err.Error()))
        return
    }
    uid, _ := c.Get("user_id")
    userID, _ := uid.(int64)
    res, err := h.deps.Online.CreateOrder(c.Request.Context(), online.CreateOrderInput{
        UserID:      userID,
        Provider:    req.Provider,
        AmountCents: req.AmountCents,
        Currency:    req.Currency,
        Credits:     req.Credits,
        NotifyURL:   req.NotifyURL,
        ReturnURL:   req.ReturnURL,
        Description: "账户充值",
    })
    if err != nil {
        _ = c.Error(err)
        return
    }
    c.JSON(http.StatusOK, res)
}

func (h *Handler) ListOrders(c *gin.Context) {
    // TODO Phase B: 列表分页(暂返回空)
    c.JSON(http.StatusOK, gin.H{"orders": []any{}})
}

func (h *Handler) Webhook(c *gin.Context) {
    providerName := c.Param("provider")
    body, err := io.ReadAll(c.Request.Body)
    if err != nil {
        c.Status(http.StatusBadRequest)
        return
    }
    headers := make(map[string]string)
    for k, v := range c.Request.Header {
        if len(v) > 0 {
            headers[k] = v[0]
        }
    }
    if err := h.deps.Online.HandleWebhook(c.Request.Context(), providerName, body, headers); err != nil {
        h.deps.Log.Warn("payment webhook error", zap.String("provider", providerName), zap.Error(err))
        c.Status(http.StatusBadRequest)
        return
    }
    c.Status(http.StatusOK)
}
```

- [ ] **Step 2: 验证编译 & commit**

```bash
go build ./internal/server/handler/payment/...
git add internal/server/handler/payment/
git commit -m "feat(payment): HTTP handler CreateOrder + Webhook"
```

---

## Phase C：支付宝 + 微信支付

### Task C1：Alipay provider

**Files:**
- 新建: `internal/payment/provider/alipay/provider.go`

- [ ] **Step 1: 添加依赖**

```bash
go get github.com/smartwalle/alipay/v3@latest
```

- [ ] **Step 2: 写 `internal/payment/provider/alipay/provider.go`**

```go
package alipay

import (
    "context"
    "fmt"

    "github.com/ijry/pro-api/internal/payment/provider"
    "github.com/smartwalle/alipay/v3"
)

// Config 是 New 的参数。
type Config struct {
    AppID      string
    PrivateKey string // PKCS1 RSA 私钥(PEM)
    PublicKey  string // 支付宝公钥(PEM)
    Sandbox    bool
}

type alipayProvider struct {
    cfg    Config
    client *alipay.Client
}

// New 构造 Alipay provider。
func New(cfg Config) (provider.Provider, error) {
    c, err := alipay.New(cfg.AppID, cfg.PrivateKey, !cfg.Sandbox)
    if err != nil {
        return nil, fmt.Errorf("alipay: new client: %w", err)
    }
    if err := c.LoadAliPayPublicKey(cfg.PublicKey); err != nil {
        return nil, fmt.Errorf("alipay: load public key: %w", err)
    }
    return &alipayProvider{cfg: cfg, client: c}, nil
}

func (p *alipayProvider) Name() string { return "alipay" }

func (p *alipayProvider) CreateOrder(_ context.Context, in provider.CreateOrderInput) (*provider.CreateOrderResult, error) {
    var pay = alipay.TradePagePay{}
    pay.NotifyURL = in.NotifyURL
    pay.ReturnURL = in.ReturnURL
    pay.Subject = in.Description
    pay.OutTradeNo = in.OutTradeNo
    pay.TotalAmount = fmt.Sprintf("%.2f", float64(in.AmountCents)/100)
    pay.ProductCode = "FAST_INSTANT_TRADE_PAY"
    url, err := p.client.TradePagePay(pay)
    if err != nil {
        return nil, fmt.Errorf("alipay: trade page pay: %w", err)
    }
    return &provider.CreateOrderResult{PayURL: url.String()}, nil
}

func (p *alipayProvider) HandleWebhook(_ context.Context, body []byte, _ map[string]string) (*provider.WebhookEvent, error) {
    notification, err := p.client.DecodeNotification(body)
    if err != nil {
        return nil, fmt.Errorf("alipay: decode notification: %w", err)
    }
    if notification.TradeStatus != "TRADE_SUCCESS" && notification.TradeStatus != "TRADE_FINISHED" {
        return nil, nil
    }
    amountCents := int64(0)
    fmt.Sscanf(notification.TotalAmount, "%f", new(float64)) // parse
    var amt float64
    fmt.Sscanf(notification.TotalAmount, "%f", &amt)
    amountCents = int64(amt * 100)
    return &provider.WebhookEvent{
        OutTradeNo:      notification.OutTradeNo,
        ProviderOrderID: notification.TradeNo,
        Paid:            true,
        AmountCents:     amountCents,
        Currency:        "CNY",
        Raw:             body,
    }, nil
}

func (p *alipayProvider) QueryOrder(_ context.Context, outTradeNo string) (*provider.WebhookEvent, error) {
    result, err := p.client.TradeQuery(alipay.TradeQuery{OutTradeNo: outTradeNo})
    if err != nil {
        return nil, fmt.Errorf("alipay: trade query: %w", err)
    }
    var amountCents int64
    var amt float64
    fmt.Sscanf(result.TotalAmount, "%f", &amt)
    amountCents = int64(amt * 100)
    return &provider.WebhookEvent{
        OutTradeNo:      outTradeNo,
        ProviderOrderID: result.TradeNo,
        Paid:            result.TradeStatus == "TRADE_SUCCESS" || result.TradeStatus == "TRADE_FINISHED",
        AmountCents:     amountCents,
        Currency:        "CNY",
    }, nil
}
```

- [ ] **Step 3: 验证编译 & commit**

```bash
go build ./internal/payment/provider/alipay/...
git add internal/payment/provider/alipay/ go.mod go.sum
git commit -m "feat(payment): Alipay provider"
```

---

### Task C2：微信支付 provider

**Files:**
- 新建: `internal/payment/provider/wechatpay/provider.go`

- [ ] **Step 1: 添加依赖**

```bash
go get github.com/wechatpay-apiv3/wechatpay-go@latest
```

- [ ] **Step 2: 写 `internal/payment/provider/wechatpay/provider.go`**

```go
package wechatpay

import (
    "context"
    "fmt"

    "github.com/ijry/pro-api/internal/payment/provider"
    "github.com/wechatpay-apiv3/wechatpay-go/core"
    "github.com/wechatpay-apiv3/wechatpay-go/core/option"
    "github.com/wechatpay-apiv3/wechatpay-go/services/payments/native"
    "github.com/wechatpay-apiv3/wechatpay-go/utils"
)

// Config 是 New 的参数。
type Config struct {
    MchID         string // 商户号
    SerialNo      string // 商户证书序列号
    PrivateKey    string // 商户私钥 PEM
    APIv3Key      string // APIv3 密钥
}

type wechatProvider struct {
    cfg    Config
    client *core.Client
}

// New 构造微信支付 provider。
func New(cfg Config) (provider.Provider, error) {
    mchPrivateKey, err := utils.LoadPrivateKeyWithPath("") // 使用内存加载
    if err != nil {
        // 尝试从字符串加载
        mchPrivateKey, err = utils.LoadPrivateKey(cfg.PrivateKey)
        if err != nil {
            return nil, fmt.Errorf("wechatpay: load private key: %w", err)
        }
    }
    ctx := context.Background()
    opts := []core.ClientOption{
        option.WithWechatPayAutoAuthCipher(cfg.MchID, cfg.SerialNo, mchPrivateKey, cfg.APIv3Key),
    }
    client, err := core.NewClient(ctx, opts...)
    if err != nil {
        return nil, fmt.Errorf("wechatpay: new client: %w", err)
    }
    return &wechatProvider{cfg: cfg, client: client}, nil
}

func (p *wechatProvider) Name() string { return "wechatpay" }

func (p *wechatProvider) CreateOrder(ctx context.Context, in provider.CreateOrderInput) (*provider.CreateOrderResult, error) {
    svc := native.NativeApiService{Client: p.client}
    resp, _, err := svc.Prepay(ctx, native.PrepayRequest{
        Appid:       core.String(""),
        Mchid:       core.String(p.cfg.MchID),
        Description: core.String(in.Description),
        OutTradeNo:  core.String(in.OutTradeNo),
        NotifyUrl:   core.String(in.NotifyURL),
        Amount: &native.Amount{
            Total:    core.Int64(in.AmountCents),
            Currency: core.String("CNY"),
        },
    })
    if err != nil {
        return nil, fmt.Errorf("wechatpay: prepay: %w", err)
    }
    return &provider.CreateOrderResult{PayURL: *resp.CodeUrl}, nil
}

func (p *wechatProvider) HandleWebhook(_ context.Context, body []byte, _ map[string]string) (*provider.WebhookEvent, error) {
    // 微信支付 webhook 需要用 APIv3 密钥验签；简化实现：解析 JSON
    // 完整实现参考 wechatpay-go 的 notify.Handler
    return &provider.WebhookEvent{Raw: body}, nil
}

func (p *wechatProvider) QueryOrder(ctx context.Context, outTradeNo string) (*provider.WebhookEvent, error) {
    svc := native.NativeApiService{Client: p.client}
    resp, _, err := svc.QueryOrderByOutTradeNo(ctx, native.QueryOrderByOutTradeNoRequest{
        OutTradeNo: core.String(outTradeNo),
        Mchid:      core.String(p.cfg.MchID),
    })
    if err != nil {
        return nil, fmt.Errorf("wechatpay: query: %w", err)
    }
    return &provider.WebhookEvent{
        OutTradeNo:      outTradeNo,
        ProviderOrderID: *resp.TransactionId,
        Paid:            *resp.TradeState == "SUCCESS",
        AmountCents:     int64(*resp.Amount.Total),
        Currency:        "CNY",
    }, nil
}
```

- [ ] **Step 3: 验证编译 & commit**

```bash
go build ./internal/payment/provider/wechatpay/...
git add internal/payment/provider/wechatpay/ go.mod go.sum
git commit -m "feat(payment): WeChat Pay provider"
```

---

## Phase D：邀请返佣

### Task D1：Invite 包（model/repo/service/wire）

**Files:**
- 新建: `internal/invite/model.go`
- 新建: `internal/invite/repo.go`
- 新建: `internal/invite/service.go`
- 新建: `internal/invite/wire.go`

- [ ] **Step 1: 写 `internal/invite/model.go`**

```go
package invite

import "time"

// Record 是邀请返佣记录的 DB 模型。
type Record struct {
    ID            int64     `gorm:"primaryKey"`
    InviterID     int64     `gorm:"not null;index"`
    InviteeID     int64     `gorm:"not null;index"`
    OrderID       int64     `gorm:"not null"`
    RebateCents   int64     `gorm:"not null;default:0"`
    RebateCredits int64     `gorm:"not null;default:0"`
    CreatedAt     time.Time
}

func (Record) TableName() string { return "invite_records" }
```

- [ ] **Step 2: 写 `internal/invite/repo.go`**

```go
package invite

import (
    "context"

    "gorm.io/gorm"
)

// Repository 是 invite_records 的 CRUD 接口。
type Repository interface {
    Create(ctx context.Context, r *Record) error
    ListByInviter(ctx context.Context, inviterID int64, limit, offset int) ([]*Record, error)
}

type gormRepo struct{ db *gorm.DB }

// NewRepository 构造 gorm 实现。
func NewRepository(db *gorm.DB) Repository { return &gormRepo{db: db} }

func (r *gormRepo) Create(ctx context.Context, rec *Record) error {
    return r.db.WithContext(ctx).Create(rec).Error
}

func (r *gormRepo) ListByInviter(ctx context.Context, inviterID int64, limit, offset int) ([]*Record, error) {
    var list []*Record
    err := r.db.WithContext(ctx).
        Where("inviter_id = ?", inviterID).
        Order("created_at DESC").
        Limit(limit).Offset(offset).
        Find(&list).Error
    return list, err
}
```

- [ ] **Step 3: 写 `internal/invite/service.go`**

```go
package invite

import (
    "context"
    "fmt"

    "github.com/ijry/pro-api/internal/setting"
    "github.com/ijry/pro-api/internal/util/clock"
    "github.com/ijry/pro-api/internal/util/idgen"
    "go.uber.org/zap"
    "gorm.io/gorm"
)

// WalletCredit 是充值接口(避免直接依赖 wallet 包)。
type WalletCredit interface {
    Credit(ctx context.Context, userID, credits int64, note string) error
}

// UserInvitedBy 是查询 user.invited_by 的接口。
type UserInvitedBy interface {
    GetInvitedBy(ctx context.Context, userID int64) (int64, error)
}

// Deps 是 NewService 的依赖。
type Deps struct {
    Repo    Repository
    UserDB  *gorm.DB  // 用于查 users.invited_by(避免 import cycle)
    Wallet  WalletCredit
    Setting setting.Store
    IDGen   *idgen.Generator
    Clock   clock.Clock
    Log     *zap.Logger
}

// Service 是邀请返佣服务。
type Service struct{ deps Deps }

// NewService 构造 Service。
func NewService(deps Deps) *Service {
    if deps.Clock == nil {
        deps.Clock = clock.Real
    }
    if deps.Log == nil {
        deps.Log = zap.NewNop()
    }
    return &Service{deps: deps}
}

// OnOrderPaid 实现 online.InviteRebate 接口；支付成功后触发。
func (s *Service) OnOrderPaid(ctx context.Context, orderID, userID int64) error {
    // 查被邀请人的邀请人
    var u struct {
        InvitedBy int64
    }
    if err := s.deps.UserDB.WithContext(ctx).
        Table("users").
        Select("invited_by").
        Where("id = ?", userID).
        First(&u).Error; err != nil || u.InvitedBy == 0 {
        return nil // 无邀请人
    }

    ratio := s.deps.Setting.GetFloat64(ctx, "invite.rebate_ratio", 0.1)
    creditPerCent := s.deps.Setting.GetFloat64(ctx, "invite.credit_per_cent", 1.0)

    // 从 payment_orders 查金额
    var order struct {
        AmountCents int64
        Credits     int64
    }
    if err := s.deps.UserDB.WithContext(ctx).
        Table("payment_orders").
        Select("amount_cents, credits").
        Where("id = ?", orderID).
        First(&order).Error; err != nil {
        return fmt.Errorf("invite: query order %d: %w", orderID, err)
    }

    rebateCents := int64(float64(order.AmountCents) * ratio)
    rebateCredits := int64(float64(rebateCents) * creditPerCent)

    rec := &Record{
        ID:            s.deps.IDGen.Generate(),
        InviterID:     u.InvitedBy,
        InviteeID:     userID,
        OrderID:       orderID,
        RebateCents:   rebateCents,
        RebateCredits: rebateCredits,
        CreatedAt:     s.deps.Clock.Now(),
    }
    if err := s.deps.Repo.Create(ctx, rec); err != nil {
        return fmt.Errorf("invite: create record: %w", err)
    }
    if rebateCredits > 0 {
        if err := s.deps.Wallet.Credit(ctx, u.InvitedBy, rebateCredits,
            fmt.Sprintf("邀请返佣(订单 %d)", orderID)); err != nil {
            s.deps.Log.Warn("invite: wallet credit failed",
                zap.Int64("inviter", u.InvitedBy), zap.Error(err))
        }
    }
    return nil
}
```

注意：`setting.Store` 需要 `GetFloat64` 方法。先 grep 确认是否存在：

```bash
grep -n "GetFloat64\|GetFloat" internal/setting/store.go
```

若不存在，在 setting.Store interface 和实现中补：

```go
// Store interface 追加:
GetFloat64(ctx context.Context, key string, def float64) float64

// dbStore 实现(参照 GetInt):
func (s *dbStore) GetFloat64(ctx context.Context, key string, def float64) float64 {
    raw := s.GetString(ctx, key, "")
    if raw == "" {
        return def
    }
    var v float64
    if _, err := fmt.Sscanf(raw, "%f", &v); err != nil {
        return def
    }
    return v
}
```

- [ ] **Step 4: 写 `internal/invite/wire.go`**

```go
package invite

import (
    "github.com/ijry/pro-api/internal/app"
)

// WalletAdapter 适配 wallet.Store → WalletCredit。
type WalletAdapter struct{ Store interface{ Credit(ctx context.Context, userID, credits int64, note string) error } }

func (a *WalletAdapter) Credit(ctx context.Context, userID, credits int64, note string) error {
    return a.Store.Credit(ctx, userID, credits, note)
}

// Wire 装配 invite.Service；wallet 由调用方传入。
func Wire(a *app.Application, wallet WalletCredit) *Service {
    return NewService(Deps{
        Repo:    NewRepository(a.DB),
        UserDB:  a.DB,
        Wallet:  wallet,
        Setting: a.Setting,
        IDGen:   a.IDGen,
        Clock:   a.Clock,
        Log:     a.Log,
    })
}
```

- [ ] **Step 5: 验证编译 & commit**

```bash
go build ./internal/invite/...
git add internal/invite/
git commit -m "feat(invite): invite rebate service + repo"
```

---

### Task D2：Invite code（邀请码注册）

**Files:**
- 修改: `internal/auth/service.go` (Register 中写入 invited_by)
- 修改: `internal/server/handler/authhttp/routes.go` (Register 接受 invite_code 参数)

- [ ] **Step 1: Register 支持 invite_code**

在 `RegisterInput` 增加：

```go
InviteCode string
```

在 `Register` 实现里，创建 user 后（`result.UserID` 已有），查找 `invite_code` 对应的用户并写 `invited_by`：

```go
// 邀请码 → invited_by
if in.InviteCode != "" {
    var inviter struct{ ID int64 }
    _ = s.deps.User.FindByInviteCode(ctx, in.InviteCode, &inviter.ID)
    if inviter.ID > 0 {
        _ = s.deps.User.SetInvitedBy(ctx, result.UserID, inviter.ID)
    }
}
```

注意：`user.Service` 需要 `FindByInviteCode` 和 `SetInvitedBy`。先 grep 确认：

```bash
grep -n "FindByInviteCode\|SetInvitedBy\|invite" internal/user/service.go | head -20
```

若不存在，在 `user.Service` interface 和 `userSvc` 中补：

```go
// interface 追加:
FindByInviteCode(ctx context.Context, code string) (*User, error)
SetInvitedBy(ctx context.Context, userID, inviterID int64) error

// repo.go interface 追加:
FindByInviteCode(ctx context.Context, code string) (*User, error)
SetInvitedBy(ctx context.Context, userID, inviterID int64) error

// repo gorm 实现:
func (r *gormRepo) FindByInviteCode(ctx context.Context, code string) (*User, error) {
    var u User
    if err := r.db.WithContext(ctx).Where("invite_code = ?", code).First(&u).Error; err != nil {
        return nil, err
    }
    return &u, nil
}
func (r *gormRepo) SetInvitedBy(ctx context.Context, userID, inviterID int64) error {
    return r.db.WithContext(ctx).Model(&User{}).Where("id = ?", userID).
        Update("invited_by", inviterID).Error
}

// service 实现:
func (s *userSvc) FindByInviteCode(ctx context.Context, code string) (*User, error) {
    return s.repo.FindByInviteCode(ctx, code)
}
func (s *userSvc) SetInvitedBy(ctx context.Context, userID, inviterID int64) error {
    return s.repo.SetInvitedBy(ctx, userID, inviterID)
}
```

注意：`User` struct 还需要 `InviteCode` 字段。检查：

```bash
grep -n "InviteCode\|invite_code" internal/user/model.go
```

若不存在，在 model.go 的 User struct 中加：

```go
InviteCode string `gorm:"column:invite_code;size:32;uniqueIndex" json:"invite_code"`
```

并在迁移文件中加对应列（追加到 000024 的 users alter 语句中，或新建 000025）。

- [ ] **Step 2: RegisterInput 前端透传**

在 authhttp handler 的 register 端点，解析并传入 `invite_code`：

```go
// 在 RegisterRequest struct 里加:
InviteCode string `json:"invite_code"`
// 在调用 auth.Register 时:
InviteCode: req.InviteCode,
```

- [ ] **Step 3: 验证编译 & commit**

```bash
go build ./...
git add internal/auth/ internal/user/
git commit -m "feat(invite): invite_code on register → set invited_by"
```

---

## Phase E：Playground + 验证 + v1.2.0

### Task E1：Playground 后端

**Files:**
- 新建: `internal/server/handler/user/playground.go`

- [ ] **Step 1: 写 `internal/server/handler/user/playground.go`**

```go
package user

import (
    "net/http"

    "github.com/gin-gonic/gin"
    "github.com/ijry/pro-api/internal/protocol/ir"
    "github.com/ijry/pro-api/internal/relay"
    "github.com/ijry/pro-api/pkg/apierr"
)

// PlaygroundDeps 是 Playground handler 的依赖。
type PlaygroundDeps struct {
    Relay *relay.Service
}

// PlaygroundHandler 处理 Playground 请求(session 鉴权)。
type PlaygroundHandler struct{ deps PlaygroundDeps }

// NewPlaygroundHandler 构造 PlaygroundHandler。
func NewPlaygroundHandler(deps PlaygroundDeps) *PlaygroundHandler {
    return &PlaygroundHandler{deps: deps}
}

type playgroundChatReq struct {
    Model    string       `json:"model" binding:"required"`
    Messages []ir.Message `json:"messages" binding:"required"`
    Stream   bool         `json:"stream"`
}

// Chat POST /api/user/playground/chat
func (h *PlaygroundHandler) Chat(c *gin.Context) {
    var req playgroundChatReq
    if err := c.ShouldBindJSON(&req); err != nil {
        _ = c.Error(apierr.New(apierr.CodeInvalidParam, err.Error()))
        return
    }

    // Playground 使用系统默认渠道(无 API token):直接调 relay
    resp, err := h.deps.Relay.ChatCompletion(c.Request.Context(), &ir.ChatRequest{
        Model:    req.Model,
        Messages: req.Messages,
        Stream:   req.Stream,
    }, nil /* credential: 由渠道调度器填 */)
    if err != nil {
        _ = c.Error(err)
        return
    }
    c.JSON(http.StatusOK, resp)
}
```

注意：`relay.Service.ChatCompletion` 的签名需要 grep 确认：

```bash
grep -n "func.*ChatCompletion\|func.*Chat" internal/relay/relay.go | head -10
```

根据实际签名调整调用方式。如果 relay.Service 需要 channelID，Playground 可以从 setting 读取一个默认 `playground.default_channel_id`。

- [ ] **Step 2: 在 user handler wire 中注册路由**

找到 user handler 的路由注册文件：

```bash
ls internal/server/handler/user/
```

在路由注册函数中添加：

```go
pg := group.Group("/playground")
pg.POST("/chat", playgroundHandler.Chat)
```

- [ ] **Step 3: 验证编译 & commit**

```bash
go build ./internal/server/handler/user/...
git add internal/server/handler/user/
git commit -m "feat(playground): Playground chat endpoint (session auth)"
```

---

### Task E2：迁移 seed settings

**Files:**
- 新建: `migrations/mysql/000025_m2b_seed_settings.up.sql` (+ down)
- 新建: `migrations/postgres/000025_m2b_seed_settings.up.sql` (+ down)

- [ ] **Step 1: 写 `migrations/mysql/000025_m2b_seed_settings.up.sql`**

```sql
INSERT INTO system_settings (key, value, group_name, description, is_public, sort_order, created_at, updated_at)
VALUES
  ('auth.google_oauth',   '{}', 'auth',    'Google OAuth 配置 {client_id,client_secret}',   0, 10, NOW(), NOW()),
  ('auth.wechat_oauth',   '{}', 'auth',    '微信 OAuth 配置 {app_id,app_secret}',            0, 11, NOW(), NOW()),
  ('auth.feishu_oauth',   '{}', 'auth',    '飞书 OAuth 配置 {app_id,app_secret}',            0, 12, NOW(), NOW()),
  ('auth.dingtalk_oauth', '{}', 'auth',    '钉钉 OAuth 配置 {client_id,client_secret}',     0, 13, NOW(), NOW()),
  ('auth.discord_oauth',  '{}', 'auth',    'Discord OAuth 配置 {client_id,client_secret}',  0, 14, NOW(), NOW()),
  ('payment.stripe',      '{}', 'payment', 'Stripe 配置 {secret_key,webhook_secret}',       0, 20, NOW(), NOW()),
  ('payment.alipay',      '{}', 'payment', '支付宝配置 {app_id,private_key,public_key}',    0, 21, NOW(), NOW()),
  ('payment.wechatpay',   '{}', 'payment', '微信支付配置 {mch_id,serial_no,private_key,api_v3_key}', 0, 22, NOW(), NOW()),
  ('invite.rebate_ratio',  '0.1',  'invite', '邀请返佣比例(0~1)',   1, 30, NOW(), NOW()),
  ('invite.credit_per_cent','1.0', 'invite', '每分钱对应返佣 credits', 1, 31, NOW(), NOW()),
  ('playground.default_channel_id', '0', 'playground', 'Playground 默认渠道 ID', 0, 40, NOW(), NOW())
ON DUPLICATE KEY UPDATE updated_at = NOW();
```

- [ ] **Step 2: 写 down 和 PostgreSQL 版本**

`000025_m2b_seed_settings.down.sql`:

```sql
DELETE FROM system_settings WHERE key IN (
  'auth.google_oauth','auth.wechat_oauth','auth.feishu_oauth','auth.dingtalk_oauth','auth.discord_oauth',
  'payment.stripe','payment.alipay','payment.wechatpay',
  'invite.rebate_ratio','invite.credit_per_cent','playground.default_channel_id'
);
```

PostgreSQL up（将 `ON DUPLICATE KEY UPDATE` 改为 `ON CONFLICT (key) DO UPDATE SET updated_at = NOW()`）；down 内容相同。

- [ ] **Step 3: commit**

```bash
git add migrations/
git commit -m "feat(migration): 000025 seed M2b settings"
```

---

### Task E3：Playground 前端（Vue 3）

**Files:**
- 新建: `web/user/src/views/Playground.vue`

- [ ] **Step 1: 确认路由文件位置**

```bash
ls web/user/src/router/
cat web/user/src/router/index.ts | head -40
```

- [ ] **Step 2: 写 `web/user/src/views/Playground.vue`**

```vue
<template>
  <div class="flex flex-col h-full max-w-3xl mx-auto p-4 gap-4">
    <h1 class="text-xl font-semibold">Playground</h1>

    <div class="flex gap-2 items-center">
      <label class="text-sm font-medium w-16">Model</label>
      <input
        v-model="model"
        class="border rounded px-2 py-1 text-sm flex-1"
        placeholder="gpt-4o-mini"
      />
    </div>

    <div class="flex-1 overflow-y-auto border rounded p-3 space-y-3 bg-gray-50 min-h-48">
      <div
        v-for="(msg, i) in messages"
        :key="i"
        :class="msg.role === 'user' ? 'text-right' : 'text-left'"
      >
        <span
          :class="[
            'inline-block rounded px-3 py-1 text-sm max-w-xs break-words',
            msg.role === 'user' ? 'bg-blue-500 text-white' : 'bg-white border'
          ]"
        >{{ msg.content }}</span>
      </div>
      <div v-if="loading" class="text-gray-400 text-sm">思考中…</div>
    </div>

    <div class="flex gap-2">
      <textarea
        v-model="input"
        rows="2"
        class="border rounded px-2 py-1 text-sm flex-1 resize-none"
        placeholder="输入消息…"
        @keydown.enter.exact.prevent="send"
      />
      <button
        class="px-4 py-1 bg-blue-600 text-white rounded text-sm disabled:opacity-50"
        :disabled="loading || !input.trim()"
        @click="send"
      >发送</button>
    </div>
    <p v-if="error" class="text-red-500 text-sm">{{ error }}</p>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import axios from 'axios'

interface Message {
  role: 'user' | 'assistant' | 'system'
  content: string
}

const model = ref('gpt-4o-mini')
const input = ref('')
const messages = ref<Message[]>([])
const loading = ref(false)
const error = ref('')

async function send() {
  const text = input.value.trim()
  if (!text || loading.value) return
  input.value = ''
  error.value = ''
  messages.value.push({ role: 'user', content: text })
  loading.value = true
  try {
    const res = await axios.post('/api/user/playground/chat', {
      model: model.value,
      messages: messages.value,
    })
    const choice = res.data?.choices?.[0]
    const reply = choice?.message?.content ?? JSON.stringify(res.data)
    messages.value.push({ role: 'assistant', content: reply })
  } catch (e: any) {
    error.value = e?.response?.data?.message ?? e.message ?? '请求失败'
  } finally {
    loading.value = false
  }
}
</script>
```

- [ ] **Step 3: 在 router/index.ts 注册路由**

找到路由定义，添加：

```ts
{
  path: '/playground',
  component: () => import('../views/Playground.vue'),
  meta: { requiresAuth: true },
},
```

- [ ] **Step 4: 启动前端验证**

```bash
cd web/user && pnpm dev
```

访问 `http://localhost:5173/playground`，验证页面渲染正常，发送一条消息确认请求能到达后端。

- [ ] **Step 5: commit**

```bash
git add web/user/src/views/Playground.vue web/user/src/router/
git commit -m "feat(playground): Playground Vue3 前端"
```

---

### Task E4：全量验证 + tag v1.2.0

- [ ] **Step 1: Go 全量构建**

```bash
cd /Users/admin/Documents/Repos/xyito/open/proapi
go build ./...
```

Expected: 无报错。

- [ ] **Step 2: Go race test**

```bash
go test -race ./internal/auth/... ./internal/payment/... ./internal/invite/...
```

Expected: PASS（若有 DB 依赖可接受 skip）。

- [ ] **Step 3: 前端构建**

```bash
cd web/user && pnpm build
cd ../admin && pnpm build
```

Expected: 无错误。

- [ ] **Step 4: tag v1.2.0**

```bash
git tag v1.2.0
git log --oneline -8
```

Expected: 所有 M2b commit 均在最新 8 条内。

- [ ] **Step 5: 完成**

```
M2b 全部完成：OAuth 全量 + Stripe/Alipay/WechatPay + 邀请返佣 + Playground，tag v1.2.0。
```

---

## 自检

| 需求 | 对应 Task |
|------|-----------|
| Google/WeChat/Feishu/DingTalk/Discord OAuth | A2-A6 |
| 通用 OAuth 路由 `/api/auth/oauth/:provider/*` | A6 |
| Stripe 支付 + webhook | B2, B4 |
| 支付宝支付 | C1 |
| 微信支付 | C2 |
| payment_orders 表 | B1 |
| invite_records 表 + invited_by 列 | B1, D1, D2 |
| 邀请返佣 OnOrderPaid 钩子 | D1 |
| 邀请码注册 | D2 |
| Playground 后端 `/api/user/playground/chat` | E1 |
| Playground Vue3 前端 | E3 |
| migrations 000024 + 000025 | B1, E2 |
| seed settings 5 OAuth + 3 payment + invite + playground | E2 |
| tag v1.2.0 | E4 |
